# det3d 模型导出（PyTorch -> ONNX）所需的最小 Python 依赖。
# 注意：torch / torchvision 需按 CUDA 版本单独安装（见 setup_det3d.sh），此处不列出。
# 仅推理（ONNX/MNN）不需要 det3d，本依赖只服务于 tools/export_onnx.py。
numpy
numba
addict
easydict
fire
terminaltables
pyyaml
opencv-python
matplotlib
scikit-image
shapely
protobuf
pillow
tqdm
onnx
onnx-simplifier
onnxruntime
